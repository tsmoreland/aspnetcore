using GlobalTicket.TicketManagement.UI.Components.ViewModels;

namespace GlobalTicket.TicketManagement.UI.BlazorWasm.App.Contracts;

public interface ICategoryDataService
{
    // TODO: use NSWAG to generate client side code from Swagger endpoint when running in debug
    //ValueTask<Page<CategoryViewModel>> GetCategoriesPage(int pageNumber, int pageSize, CancellationToken cancellationToken);
    //ValueTask<Page<CategoryEventsViewModel>> GetCategoriesWithEventsPage(int pageNumber, int pageSize, bool includeHistory, CancellationToken cancellationTokenbool);
    //ValueTask<ApiResponse<CategoryDto>> CreateCategory(CategoryViewModel categoryViewModel);
}
